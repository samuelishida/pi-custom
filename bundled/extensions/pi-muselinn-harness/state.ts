// ============================================================
// Swarm Mode — Shared Mutable State (avoid circular deps)
// ============================================================

export default {
  /** Master on/off switch */
  swarmEnabled: true,
};
