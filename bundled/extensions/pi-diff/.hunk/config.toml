# Project-local Hunk config for pi-diff review work.
# This overrides ~/.config/hunk/config.toml only inside this repo.

theme = "catppuccin-mocha"
mode = "auto"
vcs = "git"
watch = false

line_numbers = true
wrap_lines = true
agent_notes = true
exclude_untracked = false
